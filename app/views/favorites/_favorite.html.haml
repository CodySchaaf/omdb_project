= form_for(current_user.favorites.build(favorited_id: movie.id), remote: true) do |fav|
	= fav.hidden_field :favorited_id
	= hidden_field_tag :index, index
	= button_tag(style: 'display:none;') do
		%b.glyphicon.glyphicon-plus-sign